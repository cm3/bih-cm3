{
  "name": "bih-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node --import tsx ./scripts/generate-viewer-pages.ts",
    "preview": "vite preview",
    "sync-collection": "node --import tsx ./scripts/sync-bib-collection.ts"
  },
  "dependencies": {
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "citation-js": "^0.7.22",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.7"
  }
}
